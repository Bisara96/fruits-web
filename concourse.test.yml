platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: sh
  args:
  - -exc
  - cd usr/src/app/fruitweb-master && npm install && ng test --karma-config karma.conf.ci.js

inputs:
- name: fruitweb-master
  path: usr/src/app/fruitweb-master

outputs:
- name: new-release