platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: '12'

run:
  path: /bin/sh
  args:
  - -exc
  - npm install && node fruitweb-master/node_modules/@angular/cli/bin/ng test

inputs:
- name: fruitweb-master
  path: fruitweb-master

outputs:
- name: new-release