platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: node_modules/.bin/ng test
  args: ["--no-watch"]

inputs:
- name: fruitweb-master
  path: usr/src/app/fruitweb-master

outputs:
- name: new-release