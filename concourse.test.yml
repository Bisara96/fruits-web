platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: ng --version

inputs:
- name: fruitweb-master
  path: usr/src/app/fruitweb-master

outputs:
- name: new-release