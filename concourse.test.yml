platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: /bin/sh
  args:
  - -exc
  - cd fruitweb-master && npm install && ng test --karma-config karma.conf.ci.js

inputs:
- name: fruitweb-master
  path: fruitweb-master

outputs:
- name: new-release