platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: ls

inputs:
- name: fruitweb-master
  path: usr/src/app/fruitweb-master

outputs:
- name: new-release