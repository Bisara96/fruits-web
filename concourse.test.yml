platform: linux

image_resource:
  type: docker-image
  source:
    repository: angular/ngcontainer
    tag: 'latest'

run:
  path: sh
  args:
  - -exc
  - cd /home/fruitweb-master && npm install && ng test --karma-config karma.conf.ci.js

inputs:
- name: fruitweb-master
  path: /home/fruitweb-master

outputs:
- name: new-release